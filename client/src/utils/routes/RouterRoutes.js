const RouterRoutes = {
  home: "/",
  categories: "/categories",
  category: "/category/:categoryId",

  recipe: "/recipe/:recipeId",

  authentication: "/auth",

  notFound: "*",
};

export default RouterRoutes;
